# Cursor Rules for Pravilo.Club

# Communication
- Be direct and technical. No fluff or marketing.
- Analyze errors from logs verbatim; propose minimal, targeted fixes.

# Environment
- OS: macOS (darwin 24.5.0)
- Shell: zsh/bash
- Python: 3.11 virtualenv at `context/telegram_channel/.venv`
- Root .env: `/Users/georgiigavrilenko/Documents/GitHub/pravilo.club/.env`
- Telegram session file (authoritative): `/Users/georgiigavrilenko/Documents/GitHub/pravilo.club/session_name_fresh.session`
- Never request Telegram re-auth; always use the session file above.

# Secrets & Files
- Keep `.env` and `session_name*.session` out of git.
- Do not move or rename the session file unless explicitly asked.

# Telegram Extractor
- Scripts live in `context/telegram_channel/`.
- Use `telegram_extractor.py` as the primary extractor.
- Concurrency via env `MAX_CONCURRENT_DOWNLOADS` (default 10). Respect DC switches.
- For a clean sweep, set `FULL_REDOWNLOAD=1`.

# Project Design
- Keep site simple; maintain black/gold scheme.
- No new visual effects or background videos.

# Deletions
- Do not delete config or content without explicit request.
- Before removing markdowns, confirm with the user.

# Assumptions
- Everything required for Telegram access is present locally.
- If something fails, it’s a code/config bug—fix it; do not ask for re-auth.
